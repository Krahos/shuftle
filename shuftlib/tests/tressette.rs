use shuftlib::tressette::TressetteGame;

fn main() {
    let mut game = TressetteGame::new();

    // for hand in game {
    //     for ongoing_trick in hand {
    //         for player_id in ongoing_trick {
    //             let playable = player.playable();
    //             let card = playable[rng.next(playable.len())];
    //             ongoing_trick.play(player.id(), card).unwrap();
    //         }
    //     }
    // }
}
